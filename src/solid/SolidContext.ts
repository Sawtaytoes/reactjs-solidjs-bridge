import {
  createContext,
} from 'solid-js'

const SolidContext = (
  createContext({
    count: 'No count available.',
    incrementCount: (
      Function
      .prototype
    ),
  })
)

export default SolidContext
